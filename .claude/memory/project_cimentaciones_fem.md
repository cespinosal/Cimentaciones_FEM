---
name: project_cimentaciones_fem
description: Repo GitHub, clones locales (casa y VM) y plan futuro de empaquetado a .exe
metadata:
  type: project
---

Repo en GitHub: `cespinosal/Cimentaciones_FEM`. Clones locales: casa en
`C:\Users\cespi\Downloads\Hoja FEM`, y VM de Oracle Cloud en `~/Hoja-FEM`. App web sin
build/bundler/`package.json` (`index.html` + `help.html`).

El usuario planea eventualmente empaquetar la app como `.exe`, pero confirmó (2026-07-27) que es
un paso futuro — todavía no.

**Why:** decisión explícita del usuario para no invertir tiempo en algo que no pidió aún.
**How to apply:** no proponer ni iniciar el empaquetado a `.exe` salvo que el usuario lo pida
explícitamente.
